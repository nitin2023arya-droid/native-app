{
  "name": "bullion-pro",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor 6 Android App",
  "scripts": {
    "build": "echo \"No build step required\"",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}